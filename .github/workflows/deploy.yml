name: Deploy via FTPS to cyber_Folks

on:
  push:
    branches: [ main ]

env:
  NODE_VERSION: 20
  BUILD_DIR: build   # CRA -> build/

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install & Build
        run: |
          npm ci
          npm run build

      - name: Deploy to FTP (FTPS)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}      # s2.cyber-folks.pl
          username: ${{ secrets.FTP_USERNAME }}  # admin@holymiod.pl
          password: ${{ secrets.FTP_PASSWORD }}  # <Twoje nowe hasło>
          protocol: ftp
          port: ${{ secrets.FTP_PORT || 21 }}
          local-dir: ${{ env.BUILD_DIR }}/
          server-dir: public_html/
          exclude: |
            **/.git*
            **/node_modules/**
            **/.github/**
